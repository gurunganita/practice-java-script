// Reverse of number
/*
const reverse = (num) => {
  // function reverse(num) {
  if (num >= 0 && num <= 9) {
    return num;
  }
  let rev = 0;
  while (num != 0) {
    rev = rev * 10 + (num % 10);
    num = Math.floor(num / 10);
  }
  return rev;
};
*/
// console.log(reverse(14));

// const isprime = (num) => {
// if (num >= 0 && num <= 9) {
//   return num;
// }

/*while 
    if (num % 2 == 0) {
      return true;
    } else {
      return false;
    }
  }
};
console.log(isprime(9));
console.log(isprime(10));*/
